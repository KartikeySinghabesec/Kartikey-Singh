#include<stdio.h>
int factorial(int);
int main()
{
    /* int a,fact;
    printf("enter any number");
    scanf("%d",&a);

    fact=factorial(a);
    printf("factorial value = %d\n",fact);
    return 0;
}
int factorial(int x)
{
    int f=1,i;
    for(i=x;i>=1;i--)
    f=f*i;
    return(f);*/

   /* int n,reverse=0,temp;
    printf("ener the number to check the number is palindrome or not");
    scanf("%d",&n);
    temp=n;
    while(temp!=0)
    {
        reverse=reverse*10;
        reverse=;
    }
*/
/*
int x ,y,rev,m,n;
pld( x, rev, m);
{
    if(m==0)
    {
        if(n==rev)
        return 1;
        else return 0;

    }
    pld(n,(rev*10+m%10);m/10);
}
*/

/*

mean(int a[],int sum,i)
{
    if(i==5)
    return(sum/i);

mean(a,sum+a[i];i++)

}
*/

int median(int a[],int sum,i)
{
    if(l>h)
    return[--l];
    median(a,l++,h--);

}
}