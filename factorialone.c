/*#include <stdio.h>
#include <math.h>
int main()
{
    int fact = 1, i, f = 1, s, n, r = 0;
    for (i = 1; i <= n; i++)
    {

        fact = fact(n - 2) + fact(n - 1);
        printf("%d", fact);
    }

}*/
 /* f=f*i;
    r=r+f;*/
/*#include <stdio.h>
#include <math.h>
int main()
{

    int n, i,p,b, f = 1, x;
    float s = 1.00;
    printf("enter no of terms :");
    scanf("%d", &n);
        printf("enter the value of x:");
    scanf("%d", &x);
    for (i=1; i <= n - 1; i++)
    {
        p = p * x;
        f = f * i;

        b = pow(-1, i);
        s = s * (p / f);
    }
    printf("sum of series is :%0.2f", s);
}*/
#include<stdio.h>
int main()
{
    int 
}